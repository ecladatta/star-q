# Server hostname/IP address to bind to
HOSTNAME=0.0.0.0

# Port number for the container to listen on
CONTAINER_PORT=3000

# Public base URL of the application
BASE_URL=http://localhost:3000

# Instance name displayed in the header and browser title
APP_NAME=STAR-Q

# Base URI for application-specific RDF namespaces in full exports
# (annotation, at, corpus, document, statement)
RDF_NAMESPACE_BASE=https://ecladatta.eurecom.fr

# ––––––––––––––––––––––––––––––––––––––
# –––––––––––––  DATABASE  –––––––––––––
# ––––––––––––––––––––––––––––––––––––––

# Database settings
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=myusername
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydatabase
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ––––––––––––––––––––––––––––––––––––––
# ––––––––––– AUTHENTICATION –––––––––––
# ––––––––––––––––––––––––––––––––––––––

# Base public URL for Auth.js callbacks and redirects
AUTH_URL=${BASE_URL}

# Trust the host header in proxied environments
AUTH_TRUST_HOST=true

# Secret key for signing and encrypting auth tokens
# (can be generated using `openssl rand -base64 32`)
AUTH_SECRET=

# Enable self-contained username/password accounts (true/false).
# At least one of local credentials, GitHub, or Wikimedia must be configured.
LOCAL_CREDENTIALS_ENABLED=true

# GitHub OAuth application client ID
# (see https://docs.github.com/en/developers/apps/building-oauth-apps/creating-an-oauth-app#registering-your-application)
GITHUB_ID=

# GitHub OAuth application client secret
GITHUB_SECRET=

# Wikimedia OAuth application client ID
# (see https://www.mediawiki.org/wiki/Special:OAuthConsumerRegistration)
WIKIMEDIA_ID=

# Wikimedia OAuth application client secret
WIKIMEDIA_SECRET=
